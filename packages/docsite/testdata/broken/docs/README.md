# Test fixture: broken docs index

A valid link to a real file: [foo package doc](../packages/foo/doc/foo.md).

A deliberately broken link to a file that does not exist: [nonexistent page](does-not-exist.md).

An external link that must never be checked against disk: [external](https://example.com/not-a-real-path).
